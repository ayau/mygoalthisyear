<section>

    <% if (current_user){ %>
        <a class='title' href='/users/<%= current_user.id %>'><h1>My Goal this Year</h1></a>
    <% }else{ %>
        <a class='title' href='/'><h1>My Goal this Year</h1></a>
    <% } %>

    <nav role='navigation'>
        <ul>
            <% if (current_user) { %>
                <li><a href='/users/<%= current_user.id %>' ><%= current_user.name %></a></li>
                <li> | </li>
                <li>
                    <a href='/users/<%= current_user.id %>/edit' >Edit</a>
                    <ol class='notifications' style='display:none'>
                    </ol>
                </li>
                <li> | </li>
                <li><a href='/signout'>Sign out</a></li>
            <% } else { %>
                <li><a href='/auth/facebook'>Sign in with facebook</a></li>  
            <% } %>
        </ul>
    </nav>

</section>