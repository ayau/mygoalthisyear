<%= form_for Event.new, :remote => true do |f| %>
    
    <%= f.hidden_field :goal_id, :value => goal.id %>
    
    <% msg = again ? 'I did it again!' : 'I did it!' %>
    <%= f.submit msg %>
    
<% end %>